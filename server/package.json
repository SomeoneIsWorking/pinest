{
  "name": "remote-code-server",
  "version": "0.1.0",
  "description": "remote-code \u2014 control Pi coding agents remotely (pi extension)",
  "type": "module",
  "main": "src/index.ts",
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "start": "pi -e src/index.ts",
    "test": "npm run test:structure && node --test \"test/*.test.ts\"",
    "test:structure": "python3 -m unittest discover -s ../tools -p \"test_*.py\" && python3 ../tools/check_structure.py --root ..",
    "typecheck": "tsc --noEmit"
  },
  "imports": {
    "node:test": "node:test"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "typebox": "^1.3.19",
    "ws": "^8.21.0"
  },
  "optionalDependencies": {
    "cloudflared": "^0.7.1"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "@types/node": "^26.4.0",
    "@types/ws": "^8.18.1",
    "typescript": "^7.0.2"
  }
}
